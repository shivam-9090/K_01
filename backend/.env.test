# Test Environment Variables
# This file is used specifically for E2E tests to connect to the local database
NODE_ENV=test
PORT=3000

# Database - localhost for running tests on host machine
DATABASE_URL=postgresql://authuser:securepassword123@localhost:5432/auth_db
DB_USER=authuser
DB_PASSWORD=securepassword123
DB_NAME=auth_db
DB_PORT=5432

# Redis
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=change_this_redis_password_in_production

# JWT Secrets
JWT_SECRET=change_this_to_a_strong_secret_key_minimum_32_characters
JWT_EXPIRATION=900
JWT_REFRESH_EXPIRATION=604800

# 2FA
TWOFA_ENCRYPTION_KEY=un0zfCzsUJBSPdtSWAKpqkflWg4jV/dEzWT37H23AYw=

# GitHub OAuth (optional for tests)
GITHUB_CLIENT_ID=test_client
GITHUB_CLIENT_SECRET=test_secret  
GITHUB_CALLBACK_URL=http://localhost:3000/auth/github/callback

# CORS
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Email (mock for tests)
EMAIL_PROVIDER=nodemailer
EMAIL_FROM=test@example.com

# AI (optional for tests)
GEMINI_API_KEY=test_key
